"""Downloaders for the published inputs to a PMEDM problem.

Nothing here is implemented yet (README ToDo #2). The module boundaries record
where each source lives and, for the summary tables, keep an undecided access
route from leaking into callers.
"""
